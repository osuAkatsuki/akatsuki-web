import { type UserScore } from "../adapters/akatsuki-api/userScores"

export const UserProfileScores = ({
  scoresData,
  title,
}: {
  scoresData: UserScore[]
  title: string
}) => {
  return <>Profile scores here</>
}
